version: '3.9'
services:
  docuseal:
    container_name: docuseal
    image: docuseal/docuseal:latest
    ports:
    - 3000:3000
    volumes:
    - /srv/docuseal:/data
    env_file: .env
    networks:
    - caddy
    - postgres
networks:
  caddy:
    external: true
  postgres:
    external: true
